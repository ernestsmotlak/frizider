meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "email": "ernest@example.com",
    "password": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
